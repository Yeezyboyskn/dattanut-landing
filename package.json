{
  "name": "dattanut-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "generate": "node scripts/generate-pages.mjs",
    "dev": "pnpm run generate && vite --host 127.0.0.1",
    "build": "pnpm run generate && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^7.1.2"
  }
}
